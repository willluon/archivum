services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: archivum
      POSTGRES_PASSWORD: archivum
      POSTGRES_DB: archivum
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
